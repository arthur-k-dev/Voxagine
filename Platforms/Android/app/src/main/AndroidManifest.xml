<?xml version="1.0" encoding="utf-8"?>
<!--
    Deliberately minimal. This game has no network, no camera, no location and
    no file access outside its own sandbox, so it declares no permissions at
    all - which is also the cheapest possible answer to a store review.

    android.hardware.vulkan.version 0x00403000 is Vulkan 1.3. It is a hard
    requirement rather than a preference: VKDevice asks for VK_API_VERSION_1_3
    and the swapchain uses core-1.3 synchronization2 with no fallback, so a
    device without it cannot render at all. Declaring it here keeps those
    devices from being offered the app in the first place.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.vulkan.version"
                  android:version="0x00403000"
                  android:required="true" />
    <uses-feature android:name="android.hardware.vulkan.level"
                  android:version="1"
                  android:required="true" />

    <!-- Touch is the only input this build assumes. A gamepad works and is
         nicer, but requiring it would hide the app from every phone. -->
    <uses-feature android:name="android.hardware.touchscreen"
                  android:required="true" />
    <uses-feature android:name="android.hardware.gamepad"
                  android:required="false" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:isGame="true">

        <activity
            android:name=".BitBusterActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:launchMode="singleInstance"
            android:screenOrientation="sensorLandscape"
            android:resizeableActivity="false"
            android:preferMinimalPostProcessing="true"
            android:configChanges="layoutDirection|locale|orientation|uiMode|screenLayout|screenSize|smallestScreenSize|keyboard|keyboardHidden|navigation">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
