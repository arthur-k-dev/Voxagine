org.gradle.jvmargs=-Xmx4096m
android.useAndroidX=true

# The native build is a full C++ engine plus SDL3 and RTTR from source. The
# default Gradle daemon heap is not enough to also hold the CMake output.
org.gradle.parallel=true
